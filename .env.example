# Copy this file to .env and fill in your key before running the LIVE eval or the live API.
# The offline test suite and CI do NOT need this key (the LLM client is mocked).
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: port the API listens on (default 8787).
PORT=8787
